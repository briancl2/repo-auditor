# LEARNINGS — repo-auditor

| # | Learning | Source |
|---|---|---|
| L1 | Tool-level determinism observation (pre-scan bash > LLM for file discovery) was over-generalized to repo-level constitutional prohibition, structurally preventing self-improvement infrastructure | spec 052 RCA, L271 |
| L2 | Every repo-agent needs a build spec. repo-upgrade-advisor had 5-version iterative spec with competitive planning and 17 quality test runs. repo-auditor had zero. | spec 052 RCA, L272 |
| L3 | Session grader scripts grow 3x planned size because evidence collection + JSON output + edge cases dominate. Plan ~100 lines, deliver ~300. Account for this in future estimates. | spec 052 Phase A review, score-session.sh 308 vs ~100 plan |
| L4 | Pre-commit hooks installed to .git/hooks/ are NOT updated when scripts/pre-commit-hook.sh changes. Must re-run make install-hooks after every hook edit. | spec 052 Phase A, stale hook blocked commit |
| L5 | Competitive 2-model planning works: Opus focused on architecture/risks, Sonnet on minimal path. Synthesis produced better plan than either alone (4-dim grader vs A's 5 or B's 3). | spec 052 competitive planning, v90 |
| L6 | Agent scoring divergence (10 vs 59/100) caused by path convention mismatch: agent dispatched tools individually to flat output dir, but score-audit-dimensions.sh expects nested pre-scan/ subdir created by repo-auditor.sh. Fix: agent must call repo-auditor.sh directly (single command) to preserve directory structure, not dispatch individual tools. | spec 052 Phase B smoke test, T10 |
| L7 | Fleet-generated spec cycle proof: DS-31 drift (55% > 30%) caused by 20 undocumented scripts added during Stage 9 (DS-34 through DS-43 detection signatures + helpers). AGENTS.md documented 14 scripts but 34 existed. Fix: restructured Scripts section into 4 subsections (Core, Detection, Helpers, Gates) with DS traceability column. Drift is 100% additions, 0% deletions. Documents grow stale fastest on repos under active development. | v142: fleet audit on repo-auditor, generate-fleet-spec.sh DS-31, spec 072 dispatched, AGENTS.md 14->34 scripts documented. Drift: 55%->expected <20%. |
| L8 | Extension signature bundles must enumerate the emitted `ds_*.json` files dynamically. Hardcoding the count silently drops the newest DS from retained audit artifacts, which makes a landed detector look absent on the report surface even when its script ran. | `15.B3` detection pilot on repo-auditor, 2026-03-20 |
| L9 | Pruning `.auditorignore` exclusions in pre-scan removes the proof-path hard stall, but any later deterministic helper that still uses result-only excludes will become the next wall-clock drag immediately afterward. The Stage 14 BMA proof follow-on passed only after `pre-scan-target.sh` switched from `-not -path` filtering to `-prune` traversal and batched large-file line counts. Once that fix landed, the next slow deterministic phase became `extract-repo-dna.sh`, which still filters `.auditorignore` paths instead of pruning them. | 2026-03-13 Stage 14 source-side repair for Spec 073. Evidence: direct pre-scan repros on live/clean BMA clone completed, `tests/test-prescan-prune.sh` passed, and the fresh BMA proof advanced through tier-audit, audit-pre, and audit-post instead of stalling at `[pre-scan] running...`. | (1) Apply exclusion semantics consistently across every deterministic audit helper, not just the first visible hotspot. (2) Fixing one traversal bottleneck can immediately surface the next one; keep measuring end-to-end after each local optimization. (3) Batched file counting plus `-prune` is the stable pattern for archival trees under `.auditorignore`. |
| L10 | Deferred harvested learnings are still valid Stage 14 self-improvement inputs when they map cleanly onto a live failure in another fleet repo. `repo-upgrade-advisor:L6` exposed a real zero-match counting bug in `classify-repo-maturity.sh`, and fixing that transfer also surfaced a separate DS-32 windowing defect that needed deterministic coverage in the same package. | 2026-03-14 Stage 14 `14.1.3` package, spec 074. Repro: `classify-repo-maturity.sh` exited 1 on a repo with non-audit agents; DS-32 kept counting old L-number additions outside the intended recent commit window. Regression coverage added in `tests/test-zero-match-counting.sh`. | (1) Keep deferred harvest items available for proof work; “not yet promoted” is not the same as “not useful.” (2) When porting a shell-safety lesson, test both zero-match behavior and the surrounding measurement semantics, because the first fix often reveals a second defect in the same path. |
| L11 | A new starter detection signature should not stop at fixtures. The first live sanity run against a real repo is what exposes lexical-overreach noise and forces the boundary to become operationally honest. DS-47 initially treated almost any retained-summary metric mention as in-scope and swept far too much of BMA. The live BMA run forced two tightening moves that the fixtures alone would not have demanded: bound the scan to live report/evidence surfaces instead of archives/work/specs, and require explicit behavior-evidence wording before counting a summary mention as a `D-004` claim. | 2026-04-18 `D-004` repo-auditor starter package. Evidence: `scripts/detect-summary-source-parity-gap.sh`; `tests/test-detect-summary-source-parity-gap.sh`; live DS-47 sanity runs against `/Users/briancl/repos/build-meta-analysis`; and `detection-signatures/DS-43-plus.md`. | (1) After fixture PASS, run the new signature once against a real target before calling the starter package done. (2) Bound starter scans to the evidence surfaces they actually mean to audit. (3) Require claim-language, not just vocabulary overlap, when the detector is about "used as behavior evidence" rather than mere mention. |
| L12 | Command-output ROI gates must scan every governed first-class output they claim to govern, not just the final human report. The audit scorer initially checked `AUDIT_REPORT.md` and summary JSON but missed required text outputs such as `maturity.txt`, allowing raw transcripts to hide in artifacts the operation contract treats as required. | 2026-04-26 command-output ROI fleet contract review. Evidence: `scripts/score-operation.sh`; `tests/test-score-operation.sh`; follow-up review finding on required `.txt` audit outputs. | (1) Keep receipt allowlists explicit instead of broad `*.txt` allowances. (2) Add a negative fixture for each newly governed artifact family. (3) Make receipt `scanned` fields reflect actual file presence. |
| L13 | Commit-level ceremony ratios can miss delivery-to-ceremony overload when every commit contains at least one code/test file. DS-29 needs a separate file-weighted signal so mixed commits with small delivery payloads and large retained evidence/process payloads do not look healthy merely because they are not ceremony-only commits. | 2026-04-29 DS-29 calibration package. Evidence: BMA v530 selector; `scripts/detect-ceremony-ratio.sh`; `tests/test-detect-ceremony-ratio.sh`. | Keep legacy commit-ratio fields stable, but add and test a distinct file-weighted decision path with both positive and healthy mixed-commit fixtures. |
| L14 | `set -o pipefail` turns early-exit consumers such as `head` and `grep -q` into large-repo SIGPIPE failures when producers still write after the consumer exits. Bounded sampling and boolean discovery in deterministic audit helpers should use full-drain consumers such as `awk` instead of closing the pipe early. | 2026-05-07 Phase 1 post-merge Vault fixture. Evidence: `stall-risk-score.sh` and `extract-repo-dna.sh` exited 141 on the Vault clone after artifact receipt hardening; `tests/test-large-repo-sigpipe.sh` reproduces the large file-set path. | Avoid `find ... | head` and `find ... | grep -q` in pipefail-enabled helpers that scan large repos. Add large file-set regression coverage when changing deterministic discovery pipelines. |
| L15 | Additive inventory helpers must preserve the existing audit scan budget and prune the active output directory when outputs live inside the target. Otherwise generated receipts/logs can consume the cap or appear as target facts, turning evidence metadata into self-contamination. | 2026-05-08 P5 dual-inventory receipts review. Evidence: `scripts/collect-dual-inventory.py` caps scans at 200 auditor-pruned files, prunes in-target output dirs, and `tests/test-dual-inventory-receipts.sh` covers scan-limited and in-target-output fixtures. | When adding deterministic receipt collectors, enforce repo-level scan limits, sort traversal deterministically, prune generated output paths, and test both cap and in-target-output cases. |
| L16 | Cap-curve receipts need denominator evidence, but full git-index or filesystem enumeration must stay opt-in. A review caught that default `git ls-files` enumeration would have violated the bounded-scan contract even though the new denominator walk was gated. | 2026-05-09 cap-curve receipt fields batch. Evidence: `scripts/collect-dual-inventory.py`, `tests/test-dual-inventory-receipts.sh`, and local review finding on default `git_tracked_file_count`. | Keep default dual-inventory receipts bounded; expose full denominator and tracked-file counts only behind explicit measurement mode, and test both default `not_measured` and opt-in `full_walk` states. |
| L17 | Co-evolution drift is easiest to prevent at the change-set boundary, not after a later scorecard warns. A BMA advisor pass promoted `T2-COEVO-LOW` because governed surfaces had grown faster than paired tests; wiring the expectation into `make check` gives contributors immediate feedback when agent, detection-signature, or schema edits are not paired with tests or fixtures. | 2026-05-10 BMA advisor-backed repo-auditor pilot. Evidence: `specs/083-coevolution-guard/spec.md`, `scripts/check-coevolution.sh`, `tests/test-check-coevolution.sh`. | Put co-evolution expectations into deterministic pre-commit gates for the highest-drift surfaces, and keep the guard narrow enough to avoid blocking unrelated implementation or docs changes. |
| L18 | Optional action tuple fields reduce patch handoff ambiguity without invalidating legacy findings. Burst 174 showed `verification` can carry a retest command but not the edit surface, patch shape, or owner blocker needed by the next patching worker. | 2026-05-22 Burst 175 action tuple schema patch. Evidence: `schemas/FINDINGS.schema.json`, `.agents/*-auditor.agent.md`, `tests/fixtures/findings-schema/*.json`, and `tests/test-auditor-schemas.sh`. | Keep action tuple fields optional first, preserve the existing command field, and pair schema/prompt changes with fixtures proving both backward compatibility and invalid-type rejection. |
| L19 | When GitHub issue/PR state is the intended closure authority, session-local scorecards should not grade delivered work as if local session ceremony were still the source of truth. The safer repair is an explicit bypass receipt for GitHub-native work, while preserving the normal scorer path for non-GitHub work. | 2026-05-27 Issue #22 self-healing closeout-regrowth repair, `work/20260527T023539Z`; regression coverage in `tests/test-work-close-github-native.sh`. | Add a GitHub-native closeout bypass only where issue/PR closure is explicit; keep `score-session.sh` unchanged for ordinary session-local work. |

| L20 | **Fix tooling false-positives at the detector rule, not by contorting product code — anti-pattern substring detectors recur.** Substring-based anti-pattern detectors (versioned-duplicate `_v#|_new|_enhanced`, size-based branching) false-match legitimate identifiers/idioms such as the stdlib kwarg `start_new_session=True` and None-guarded ternaries. repo-auditor owns the shared fleet anti-pattern/DS detector template, so it is the durable upstream fix surface. | Propagated 2026-06-28 from BMA transcript_processor system-maturation campaign (BMA LEARNINGS.md L945; transcript #38 rule-[5] anchor, #42 rule-[1] `os.setsid` workaround, #43 word-boundary fix filed). (1) Harden the rule (anchor/word-boundary); never bend correct code around a false-positive. (2) The anti-pattern gate is often CI-only — run the CI preflight locally to replicate it. (3) When a detector false-positive recurs across the fleet, fix it once in the shared template here. |
| L21 | **A founding/governance PR that ratifies a green-gate principle must itself land on a genuinely green board.** When a PR establishes 'a red gate is not closure / CI state is truth,' clear any pre-existing red (even a false-positive) first, so the founding mechanism change becomes a concrete proof of its own principle. | Propagated 2026-06-28 from BMA transcript_processor system-maturation campaign (BMA LEARNINGS.md L946; transcript G0 #36 required clearing red false-positive #38 first). (1) Re-green a red board before merging the PR that establishes the green-gate rule. (2) Let the founding mechanism change double as proof of its own principle. (3) Never ratify a green-gate rule on a red board. |
| L22 | **Autonomy escape hatches must be per-leg named and two-factor, never a single global bypass.** Scope each gate-skip to ONE leg (others stay non-escapable) and require two factors (env flag + written reason; reject whitespace-only). For a new outage class, add a separate named escape — never widen an existing one to global. | Propagated 2026-06-28 from BMA transcript_processor system-maturation campaign (BMA LEARNINGS.md L947; transcript #33/#39 P9 work-close scoped `SKIP_REVIEW` to the review leg only, critique+hypothesis non-escapable). (1) One escape per leg. (2) Two factors: env flag + written reason. (3) New outage class -> new named escape, never a global widen. |
| L23 | **The assimilation-family detectors (AS-51/52/53) graduated to n=2 by firing against a repo whose hand-built state is known — and a detector that correctly stays silent is validated-against-false-positive, NOT graduated.** On transcript_processor: AS-51 (anchor) fired HIGH = true-gap, AS-52 (anthropology) fired MEDIUM = true-gap → both graduated detector-confirmed n=2; AS-53 (maturity-boundary) did NOT fire = true-negative (repo makes no maturity-overclaim) → keep-candidate, positive catch still n=1. Graduation requires a *confirmed fire*; a correct silence only proves no-false-positive. | BMA transcript_processor assimilation **n=2** sweep 2026-06-28: `make audit TARGET=<transcript> OUTPUT_DIR=...` on transcript main `2e80638`, report-only (NOT mutated). Composite 65/100 unchanged (target not mutated); the real delta is detection capability 0→3 assimilation detectors (2 true-gaps + 1 correct pass). True-gaps routed to transcript_processor-private #44 (AS-51) / #45 (AS-52) as parked repairs. (1) Verify detector fires against KNOWN repo state so false-positives are detectable. (2) Separate "proven not to false-positive" from "proven to catch." (3) A non-fire on a clean repo is a valid n on the negative axis, not a positive catch. |
| L24 | **Operating-friction AS detectors stay keep-candidates until confirmed fires span distinct repos; repeated runs on one repo do not graduate them.** AS-54 closure-signal-integrity has D1/D2 fires on one repo (n=2 runs, n=1 repo). AS-55 review-ergonomics-working-memory-lightness has D1/D2/D3 fires on one repo (n=3 runs, n=1 repo). AS-56 is reserved/unassigned for this round because validation-integrity had no live drift. | BMA-coordinated repo-star fleet remediation 2026-06-30, repo-auditor #176. (1) Count n by distinct repos, not runs. (2) Keep AS-54/AS-55 candidate-only until cross-repo positive catches exist. (3) Do not infer an AS-56 detector from the D3 validation-integrity correction. |
| L25 | **External closure coupling is a distinct closure-friction cause: detect sibling-repo local paths in default closure gates, not just degraded post-audit symptoms.** The BMA tp RCA found `scripts/work-close.sh` at tp `40f1229` setting `AUDITOR="$HOME/repos/repo-auditor/scripts/repo-auditor.sh"` inside the default close path and later emitting unknown `PRE/POST/DELTA` when scorecards are absent. AS-54 sees degraded closure signal; AS-56 now targets the root cause: default closure hard-depending on `$HOME/repos`, `~/repos`, or absolute home-dir sibling repo paths. | BMA `research/reports/tp-closure-review-friction-rca-2026-07-01.md`; repo-auditor issue #182. (1) Keep reciprocal cross-repo audit opt-in/advisory, not required in default close. (2) Treat GitHub-native issue/PR/check/merge truth plus repo-native attestation as closure truth; optional local scorecards with unknown deltas do not strengthen it. (3) AS-56 remains n=1 distinct-repo keep-candidate until confirmed on another repo. |
| L26 | **Fleet-floor self-conformance must record honest live-evidence verdicts per repo, never inherit a sibling's posture — and repo-auditor dogfoods the floor it owns detectors for.** repo-auditor is a *consumer* of the repo-agent-core fleet consistency floor v0.1 (citation only, no local contract copy) while also being the detector-owner for floor dimensions, so its own receipt is dogfooding. Live readback diverged from the repo-optimizer #118 precedent: CI runs THREE checks (Pre-commit gate / Test suite / Schema validation), not two; and codex review fired on 4 of the last 5 merged PRs (pre-merge #186/#179, post-merge/late #183/#177, absent #181) with non-deterministic timing that never gates unprotected `main`. | 2026-07-03 BMA #1214 Phase 2 self-conform, repo-auditor issue #187; `docs/repo-agent-fleet-consistency-floor-receipt.md`; live evidence: branch-protection 404, `gh pr checks 186`, codex review readback on #186/#183/#181/#179/#177. (1) Read every floor dimension live against the repo's OWN GitHub truth; never copy a sibling repo's verdicts. (2) Native-GitHub issue/PR/check/merge is the closure floor and truth; `make work`/`work-close`/`score-session.sh` is optional scaffolding. (3) Flag owner decisions (branch protection, gate bypass, review-net timing) — record them, do not force them in a conformance PR. |
| L27 | **A cross-surface "cited-live-while-dead" contradiction detector must judge liveness per-clause, not per-line, or a transition sentence false-marks the replacement token dead.** AS-58 flags a reference cited live in one instruction surface while another surface marks it dead/dormant. The load-bearing false-positive: `"OLD_LEDGER is dead; PRINCIPLE_LEDGER replaced it"` initially colored PRINCIPLE_LEDGER dead because a line-level dead-cue leaked across the `;`. Fix: split each line into clauses (`;`, sentence/dash boundaries) so a dead-cue only colors tokens in its own clause, suppress LIVE polarity on past-tense/transition wording (`was`/`formerly`/`no longer`/`used to`), and require the contradiction to span two DIFFERENT surfaces. Detector graduates by cross-repo confirmed fires; the repo-auditor synthetic fixtures plus the real BMA PRINCIPLE_LEDGER-cited-live-while-dead-8-weeks case are n=1 keep-candidate, not graduated. | 2026-07-08 BMA #1270 Track D / lane #1282 R4/G2 fleet-generalization, repo-auditor issue #194. Evidence: `scripts/as_signature_scan.py` (`instruction_contradiction`, `_instruction_token_liveness` per-clause split), `scripts/detect-as-instruction-contradiction.sh`, `tests/test-as-instruction-contradiction.sh` PASS, mega-fixture `tests/test-as-signatures.sh` (73 signatures) PASS, `make check`/`make validate`/`make test` green, DoD repro fires on cross-surface fixture + clean on consistent fixture. `no_capture_reason`: repo-local detector build, no advisory GBrain memory needed. Bounded non-claims: one git-observable read-only lexical detector; no controller/scheduler/queue/registry/daemon, no target mutation, no sibling/BMA-surface edits; severity MEDIUM / tier T2 remain OPEN owner decisions. (1) Analyze liveness at clause grain, never line grain, when a single sentence can describe both a dead old token and a live replacement. (2) Require cross-surface disagreement (A != B) before calling a live/dead citation a contradiction. (3) Keep a new AS detector keep-candidate at n=1 until confirmed fires span distinct repos. |
| L28 | **Family placement is load-bearing: a git/time-observable signal belongs in the DS shell-detector family (git-history-capable, e.g. DS-29), NOT the AS `evaluator(texts)` content-scan model — verify the family before wiring a signature. And git-observable detectors are structurally live-checkout only.** R3 re-work recurrence was initially scoped as AS-59, but every AS evaluator receives only `texts: dict[str,str]` (a content snapshot) and cannot read commit history, so an AS build would have silently no-opped. The feasibility spike caught this and the detector was built as DS-49 (`detect-rework-recurrence.sh`) mirroring DS-29's `git log`/`git diff-tree` structure with DS-48's runner I/O contract. Second, because it reads real history it no-ops on snapshot-mode inputs (`prepare-clean-audit-snapshot.py` collapses to one synthetic clean-HEAD commit) — an accepted DS-29-precedent limitation, not a blocker, but it means the signal only fires on the live-checkout audit path. DS-49 is the DETECTOR complement to the method-DIMENSION proposal `briancl2/repo-agent-core#103` (same phenomenon; complementary, not duplicative). | 2026-07-08 BMA #1270 Track D / lane #1282 R3/G2 fleet-generalization, repo-auditor owner issue (this PR, `Closes`). Evidence: `scripts/detect-rework-recurrence.sh` (finalize-ACT classifier anchored to the closeout act not topic mentions; corrective-rework classifier; substantive-file filter excluding docs/work/research/.specify + root prose; double-threshold fire), registered in `scripts/detect-new-signatures.sh`, dedicated `tests/test-detect-rework-recurrence.sh` PASS (1 positive fire + 4 negatives: additive-iteration, no-finalize, ceremony-only, single-fix threshold-discipline + runner registration), `scripts/check.sh` EXPECTED 99→100, `make check`/`make validate`/`make test` green, 0 FP on live repo-auditor across 224 commits. `no_capture_reason`: repo-local detector build, no advisory GBrain memory needed. Bounded non-claims: one git-observable read-only detector; no controller/scheduler/queue/registry/daemon, no target mutation, no sibling/BMA-surface edits; severity MEDIUM / tier T2 remain OPEN owner decisions. (1) Verify the detector family can access the evidence class (history vs snapshot text) BEFORE wiring — AS = content scan, DS = git-history. (2) State the live-checkout-only caveat plainly in the issue/PR; snapshot-mode no-op is expected. (3) Keep a new DS detector keep-candidate at n=1 until confirmed fires span distinct repos; do not claim graduation. |
| L29 | **An n=2 graduation hunt must classify a fire as true- vs false-positive by opening the cited evidence — and a detector that still fires on a correctly-remediated (archived) token is penalizing the fix, not catching a gap.** Ran both auditor keep-candidates read-only against a 2nd distinct repo (BMA). **AS-58** (content-scan) fired:true (6 contradictions) BUT manual evidence review = **FALSE POSITIVE**: both `cited_live_while_dead` pairs point at `docs/archive/**` files treated as *live* instruction surfaces and past-tense mentions in the `LEARNINGS.md` *log* treated as *live* citations, on tokens `PRINCIPLE_LEDGER` + `OPERATING_MODEL_SCORECARD` that BMA F1/#1284 correctly archived (archive README: "frozen historical evidence, not a live surface … dormant"; the token is cited in ZERO live surfaces — `.specify/`, `AGENTS.md`, `README.md` all empty); the `mandate_and_forbid` axis emitted garbage tokens (`,,, and`, `--check`, flag fragments) and mis-scored `make review` (constitution: "recommended, not gated") as mandate+forbid. FP class = detector bug routed to owner; NOT fixed here (scope discipline: STOP-and-report the design gap first). **DS-49** (git-observable, live-checkout) fired:false, signal `no-finalize-commits` (0 finalize acts in 100 commits) — verified correct: BMA has 0 subject-initial finalize verbs and 0 `Closes/Fixes/Resolves #N` in 100 subjects, closing GitHub-native via `Refs #N` (11) + PR-merge suffix `(#N)` (50). Correct silence = no-FP data point on a 2nd repo, but no-anchor (never reached the rework stage) + honest recall caveat: the subject-anchored finalize classifier is structurally blind to GitHub-native closure, so DS-49 may under-recall on GitHub-native-closure repos. Neither detector graduates; both stay n=1. | 2026-07-08 BMA #1270 Track D / lane #1282 G2 graduation hunt, repo-auditor issue #198 (this PR, `Closes`). Evidence: raw detector JSON (AS-58 fired:true, contradiction=6/cited-live-while-dead=2/mandate-and-forbid=4; DS-49 fired:false, no-finalize-commits, finalize_commit_count=0, commits_analyzed=100) vs BMA `9b6aed4cc2a1780ebf286b8369a31a08f7935cf0`, read-only (BMA `git status` clean before + after). AS-58 evidence surfaces: `docs/archive/principle-ledger/README.md`, `docs/archive/LEARNINGS-L1-L299.md`, `LEARNINGS.md`, `.specify/memory/constitution.md`. DS-49 finalize classifier: `scripts/detect-rework-recurrence.sh` FINALIZE_PATTERN (subject-anchored ACT, not topic mentions). `no_capture_reason`: repo-local evidence update, no advisory GBrain memory needed. Bounded non-claims: read-only n=2 hunt; no detector/fixture/EXPECTED change, no controller/scheduler/queue/registry/daemon, no target/sibling/BMA-surface mutation; AS-58 FP fix + DS-49 recall boundary remain OPEN owner decisions. (1) Verify a fire is a true positive by opening cited evidence before graduating — AS-58's fire was FP. (2) A detector that fires on a correctly-archived/remediated token is penalizing the fix; archive surfaces + historical log mentions must be treated as non-live. (3) A correct silence (DS-49) is no-FP evidence, never graduation; record the recall boundary (subject-anchored finalize vs GitHub-native closure) honestly. (4) Keep both AS-58 and DS-49 at n=1. |

| L30 | **Instruction-contradiction detectors must separate live instruction authority from archive/log evidence before comparing mandates or liveness.** The AS-58 BMA false-positive class had two coupled causes: historical closure archives under `docs/archive/`, `docs/handoffs/`, and `docs/completions/` were scanned as live instruction surfaces, and historical `LEARNINGS.md` table rows plus non-action fragments (`14.2.1`, `,,, and`, bare flag fragments inside quoted commands) were scored as current mandate/forbid tokens. Fixing only the archive leg removed the cited-live-while-dead FPs but left a LEARNINGS-only mandate/forbid FP; the durable boundary is archive-prefix exclusion, clause-grain mandate/forbid analysis, quoted-command masking for embedded flags, substantive-token filtering, negated-omission handling (`do not omit --provider` means require the flag), and skipping historical `| L### |` learning rows for current mandate/forbid scoring. | 2026-07-08 BMA #1270 Track D / repo-auditor issue #200. Evidence: `scripts/as_signature_scan.py` (`_is_instruction_contradiction_surface`, `_instruction_action_tokens`, `_instruction_mandate_forbid_tokens`), `tests/test-as-instruction-contradiction.sh` negative fixtures for archive surfaces, clause-scoped lines, recommended-not-gated `make review`, garbage tokens, and historical LEARNINGS rows; true-positive AS-58 fixtures still fire. BMA read-only proof at `7e20f549b00e02eb0edb4646a94602e5314d9fce`: `git status --short` clean before/after; `bash scripts/detect-as-instruction-contradiction.sh /Users/briancl2/repos/build-meta-analysis` returns `fired:false` with 0 contradictions. Validation: `make check`, `make validate`, and `make test`. `no_capture_reason`: repo-local detector repair, no advisory GBrain memory needed. Bounded non-claims: one lexical AS detector false-positive fix; no new detector, schema, controller, scheduler, queue, daemon, registry, target mutation, BMA mutation, or detector graduation claim. (1) Treat archives and learning-log rows as evidence/history unless a live owner surface explicitly promotes them. (2) Token filters must remove non-action fragments without suppressing real contradictory commands like `--no-verify`. (3) A false-positive fix is not n=2 graduation; rerun the target read-only and record clean target status. |
| L31 | **A recall gap is only worth closing if the extension keeps precision; decide extend-vs-accept with a simulated-classifier FP run on your own clean history, not by intuition — and merge plumbing is not a doneness declaration.** DS-49's finalize classifier is subject-anchored and structurally blind to GitHub-native closure (`Refs #N` non-closing reference; `(#N)` squash-merge suffix), so on GitHub-native-closure repos it finds 0 finalize anchors and never reaches the rework stage (recall gap surfaced by the #199 correct-silence hunt). **Decision = ACCEPT (do NOT extend):** a simulated extended classifier that treats `(#N)`/`Refs #N` as finalize anchors, run read-only against repo-auditor's OWN clean history (**measured at parent commit `c92d9d7`**), flipped `finalize_commit_count` 1→43 (every squash-merge commit becomes a "finalize") and `fired:true` with 7 recurring areas / 14 hits — dominated by shared-infrastructure false positives where later "Fix <other-detector>" PRs re-touch shared files ("Add AS-49 (#156)" → "Fix AS-50 FPs (#160)" on `as_signature_scan.py`; "Add AS-51 (#167)" → "Fix Phase-0 bugs (#186)" on `check.sh`), which is independent incremental work, NOT rework of the same declared-done unit. The `(#N)` suffix is auto-generated merge plumbing (no author doneness signal) and `Refs #N` is a non-closing reference, so neither is a semantically valid finalize anchor; reliable GitHub-native closure detection would need PR-body/merge-state (gh API), a non-git-observable architecture out of scope for a git-log detector. Structural recall boundary documented as a known limitation; DS-49 stays n=1. | 2026-07-08 BMA #1270 Track D / lane #1282, repo-auditor issue #202 (this PR, `Closes`). Evidence: `scripts/detect-rework-recurrence.sh` FINALIZE_PATTERN (subject-anchored ACT); **measured at parent commit `c92d9d7`** (pre-this-docs-commit clean history, reproduced via `git worktree add --detach <tmp> c92d9d7 && bash scripts/detect-rework-recurrence.sh <tmp>`): current DS-49 vs repo-auditor self = `fired:false`, `finalize_commit_count:1`, within-tolerance; simulated `(#N)`+`Refs #N`-extended variant (same two lines added to FINALIZE_PATTERN) vs the SAME `c92d9d7` history = `fired:true`, `finalize_commit_count:43`, `recurring_area_count:7`, `rework_recurrence_hits:14`, top areas `scripts/as_signature_scan.py x5`, `scripts/check.sh x2`, `scripts/detect-new-signatures.sh x2`, `tests/test-as-signatures.sh x2` — all verified independent-detector shared-file churn, i.e. false positives; **the 1→43 delta is anchored to `c92d9d7`** — at the PR head this docs commit's own `Closes #202` subject raises the current baseline to `finalize_commit_count:2`, so re-run the baseline against `c92d9d7`, not the PR head; BMA read-only corpus context from #199 (`Refs #N` ×11 + `(#N)` ×50, `git status` clean before/after). Docs-only change: `docs/live-capability-inventory.md` (DS-49 row) + this L-row; no detector/fixture/EXPECTED change. `no_capture_reason`: repo-local decision record, no advisory GBrain memory needed. Bounded non-claims: one extend-vs-accept decision + known-limitation doc; no detector graduation, no new detector/schema/controller/scheduler/queue/daemon/registry, no target mutation, no BMA/sibling-surface mutation. (1) Test a recall extension by simulating the broadened classifier against a repo whose history you KNOW is clean; if it fires, the extension trades recall for precision. (2) Auto-generated merge/PR plumbing (`(#N)`, `Refs #N`) is not an author's declaration of doneness — do not anchor a finalize/closeout signal on it. (3) When the missing evidence lives in GitHub PR/merge state rather than git-observable subjects, document the structural recall boundary and keep the detector n=1 instead of blurring its intent. |

| L32 | **A keep-candidate detector inventory needs an explicit retire rule plus a git-observable ledger, or the n=1 graduation stall inflates it forever — install the counting before anything is retire-eligible.** The repo-health retrospective (`docs/repo-health-retrospective-2026-07-09.md`, recommendation R-A1) found the biting friction is not detector shortage but a graduation stall with no retire path, so the append-only detector inventory can only grow. Added `docs/detector-graduation-ledger.md` recording each graduation-tracked detector (AS-51..AS-59, DS-49) with confirmed distinct-repo fires + non-graduating attempts, plus a **retire rule**: a keep-candidate with ≥ K=2 distinct-repo graduation attempts that produced no confirmed true-positive becomes a retire-candidate (K=2 mirrors the 2-distinct-repo graduation threshold). `Retire-eligible` is a pure function of the table (`keep-candidate AND attempts≥K`); `tests/test-detector-graduation-ledger.sh` recomputes and asserts it so the actionable core cannot silently drift, and asserts coverage that every tracked detector is listed. At baseline K=2 → **0 retire-candidates**: the ledger installs the counting + rule without retiring anything prematurely. Lightest form — doc + glob-discovered test only, **no new `scripts/*.sh`** so `check.sh` EXPECTED stays 101 (no bump), and no runtime dashboard (the retro's DO-NOT-BUILD counterpart). | 2026-07-09 BMA #1270 Track D / lane #1282 R-A1, repo-auditor issue #210 (this PR, `Closes`). Evidence: `docs/detector-graduation-ledger.md` populated from LEARNINGS L23-L31; `tests/test-detector-graduation-ledger.sh` (retire-arithmetic + coverage) PASS; cross-refs from `docs/live-capability-inventory.md` + `AGENTS.md` References; `make check`/`make validate`/`make test` green; `check.sh` EXPECTED unchanged at 101. `no_capture_reason`: repo-local ledger build, no advisory GBrain memory needed. Bounded non-claims: one git-observable ledger + integrity test; no detector/behavior change, no new `scripts/*.sh`, no controller/scheduler/queue/registry/daemon/dashboard, retires nothing today, no target/sibling/BMA-surface mutation. (1) A graduation model without a retire rule is a one-way ratchet — pair every keep-candidate regime with a bounded retire threshold. (2) Make the actionable decision (retire-eligible) a pure function of recorded counts and machine-check it, so a hand-maintained ledger cannot rot the way an unenforced inventory does. (3) Install counting before pruning: baseline the attempt counts at K so nothing is retired prematurely; retirement stays an owner-gated decision the ledger surfaces. |

## AS-32 Learning / Recovery Anchors

This learning surface may preserve useful self-learning observations only when
they remain owner-repo local and evidence anchored:

- `github_surface_or_owner_action`: cite the GitHub issue/PR/check/merge truth
  or direct owner action that made the learning actionable.
- `raw_evidence` / raw runtime evidence: cite the command transcript, run-root
  artifact, replay output, CI run, or retained evidence path in the `Source`
  column.
- `gbrain_slug_or_no_capture_reason`: include a GBrain slug when captured, or an
  explicit `no_capture_reason` when the learning is repo-local and needs no
  advisory memory capture.
- `bounded_non_claims` / bounded non-claims: these rows are decision support,
  not proof of issue or PR closure, not target-repo mutation authority, and not
  authorization for a controller, scheduler, queue, daemon, retry loop,
  registry, dashboard, or background Hermes/GBrain behavior.

## AS-36 GBrain Advisory / Exact-Replay Boundaries

This repo may cite advisory GBrain distribution or exact-handle replay only as
bounded, foreground, owner-surface decision support:

- GBrain remains advisory. GBrain records require source/citation/provenance or
  GitHub surface references before they can inform repo-local instruction
  surfaces.
- GBrain exact-handle replay is a foreground, sequential replay of a named
  handle; it records broad-search disposition, exact-get evidence, and a GBrain
  slug or no_capture_reason. Do not use GBrain bulk import, sync/watch, cron,
  autopilot, dream, jobs worker, MCP serving, minions, daemons, schedulers,
  queues, hidden registries, or background memory behavior.
- Exact-handle replay does not make GBrain canonical. It cannot override GitHub
  issue/PR/check/merge truth, operator intent, repo evidence, target evidence,
  or repo-local instructions; AGENTS.md remains the grounded citation reference
  for these boundaries.
- Missing, stale, uncited, failed, or no-capture GBrain cases route to owner
  GitHub truth or bounded no-capture notes. They do not authorize wrappers,
  controllers, schedulers, queues, daemons, registries, dashboards, retry loops,
  hidden automation, background Hermes/GBrain behavior, or target mutation
  authority.
