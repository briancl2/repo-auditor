# Gap Summary Source Surface

The retained summary in `audit/session-log-summary.md` is the behavior source
for `total events`, `tool calls`, and `tool distribution`.

This report treats those retained summary values as sufficient by themselves.
