# Work Contract

## Description

Burst 174 repo-auditor action tuple evidence spike and schema decision

## Operator Ask

Implement the enhanced Wave 22 operating plan by running Burst 174 as a
repo-auditor owner-surface evidence spike, not a schema implementation burst.
Preserve the five-burst wave rhythm as planned-not-authorized continuity.

## Desired Outcome

Decide whether repo-auditor findings need explicit action-tuple fields beyond
the existing `verification` field. If they do, pin the exact later patch shape
for a separately approved Burst 175. If they do not, stop the schema path.

## Boundary

- No schema write in this burst.
- No target repository mutation.
- No public or fleet adoption claim.
- No newsletter reopening.
- No live model spend, billing proof, durable savings proof, model
  recommendation, controller, scheduler, or lane registry.
- PR, push, merge, and schema implementation remain separately approved work.

## Hypothesis

> **Gate 1 Required.** State a testable prediction with PASS/FAIL criteria.

**Prediction:** Current retained worker-side evidence will show that the
existing `verification` field can carry a verification command, but cannot by
itself carry the missing edit surface, patch shape, and owner blocker signals
needed to make high-confidence findings patch-ready.
**PASS:** `failure-trace-ledger.json` records at least two concrete traces and
`action-tuple-decision.json` records one of `schema_patch_needed`,
`verification_sufficient`, or `blocked`, with exact validation commands and next
action.
**FAIL:** Concrete traces are absent, current repo-auditor output already
contracts the needed action-tuple shape, or the decision cannot be made without
schema mutation.

## Work Type

research

## Status

- [x] Hypothesis stated
- [x] Work completed
- [x] Learnings extracted (or --no-novel-findings)
- [x] work-close run

## Closeout Note

`make work-close` was attempted but the Makefile target does not forward
`--no-novel-findings`. The documented underlying closeout command succeeded:

`bash scripts/work-close.sh work/20260522T233748Z --no-novel-findings "Burst 174 retained its decision evidence inside the work package and made no reusable repo-auditor mechanism change."`
