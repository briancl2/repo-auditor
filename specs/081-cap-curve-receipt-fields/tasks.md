# Tasks: Cap-Curve Receipt Fields

1. Add run timing fields to `AUDIT_RUN_RECEIPT.json`.
2. Add opt-in dual-inventory denominator and coverage fields.
3. Preserve default bounded scan behavior and non-authorization semantics.
4. Document the receipt additions and environment variables.
5. Extend focused tests for receipt timing and denominator coverage.
6. Run focused tests, `make test`, and `make check`.
